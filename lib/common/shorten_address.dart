String shortenCharacters(String input) {
  return input.length > 20 ? input.substring(0, 10) : input;
}

String shortenAddress(String input) {
  return input.length > 20 ? input.substring(0, 10) : input;
}
